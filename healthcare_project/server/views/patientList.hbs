<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
    <title>Login</title>
</head>

<body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="/"> &nbsp; Healthcare System</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/DoctorDiagnosissList">Diagnosis</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/patientList">Patient</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/doctorPersonalInfo">Personal Information</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/logout">Logout</a>
                </li>
            </ul>
        </div>
    </nav>

    <div class="row userlist">

        {{#each patients}}
        <div class="col-sm-6">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">id: {{this.id}}, {{this.name}}</h5>
                    <h6 class="card-subtitle mb-2 text-muted">Gender: {{this.gender}}</h6>
                </div>
                <ul class="list-group list-group-flush">
                    <li class="list-group-item">Phone: {{this.phone}}</li>
                    <li class="list-group-item">Email: {{this.email}}</li>
                </ul>
                <div class="card-footer">
                    <small class="text-muted">{{this.type}}</small>
                </div>
                <div class="card-body">
                    <form action="/addDiagnosis" method="POST">
                        <input type="hidden" value="{{this.id}}" name="patient_id">
                        <input type="hidden" value="{{this.name}}" name="name">
                        <button type="submit" class="btn btn-outline-secondary">Add Diagnosis</button>
                    </form>
                </div>
            </div>
        </div>
        {{/each}}

    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
        crossorigin="anonymous"></script>
</body>

</html>